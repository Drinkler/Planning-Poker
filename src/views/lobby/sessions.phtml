<div class="container">
    <div class="row">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Host</th>
                    <th scope="col">Name</th>
                    <th scope="col">Created</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->vars as $key => $lobby): ?>
                    <tr>
                        <th scope="row"> <?= $key ?></th>
                        <td><?= $lobby["uname"] ?></td>
                        <td><?= $lobby["name"] ?></td>
                        <td><?= $lobby["created"] ?></td>
                        <td>
                            <div class="">
                                <form method="post" action="<?= $this->makeURL("lobby/join") ?>" style="display:inline;">
                                    <button type="submit" class="btn btn-outline-primary">Join</button>
                                </form>
                                <?php if ($lobby["creator"] == \PlanningPoker\Library\Session::get("user")->getId()): ?>
                                &nbsp;
                                <form method="post" action="<?= $this->makeURL("lobby/delete") ?>" style="display:inline;">
                                    <button type="submit" class="btn btn-outline-danger">Delete</button>
                                </form>
                                <?php endif; ?>
                            </div>
                        </td>
                    </tr>
                <?php endforeach;?>
            </tbody>
        </table>
    </div>
</div>